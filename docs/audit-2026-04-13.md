claude code report, after archival work:

● ---
  Cleanup + Archive Session — Deliverables

  Job 1 — Archive (DONE)

  What I moved

  278 files / ~3.1 MB relocated to /home/tron/dev/projects/agent-cc/archive/:

  ┌─────────────────────────┬──────────────────────────────────────────────────────────────┬────────────────────┐
  │      Subdirectory       │                           Contents                           │       Count        │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────┼────────────────────┤
  │ docs-superpowers/plans/ │ Historical implementation plans (git-tracked, now removed)   │ 25 files           │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────┼────────────────────┤
  │ docs-superpowers/specs/ │ Design specs / brainstorm outputs (git-tracked, now removed) │ 33 files           │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────┼────────────────────┤
  │ docs-audits/            │ 2026-04-13-flat-to-tree-audit.md                             │ 1 file             │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────┼────────────────────┤
  │ roadmap-milestones/     │ 43 completed milestone directories from .claude/roadmap/     │ 187 task .md files │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────┼────────────────────┤
  │ roadmap-workflow-index/ │ ROADMAP.md, MILESTONE.md, TASK.md, ARCHIVE.md                │ 4 files            │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────┼────────────────────┤
  │ roadmap-drafts/         │ Stale .claude/roadmap/drafts/                                │ 3 files            │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────┼────────────────────┤
  │ brainstorm/             │ All 15 .superpowers/brainstorm/ session directories          │ 15 dirs            │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────┼────────────────────┤
  │ root-docs/              │ AUDIT.md, BRANDING.md, REPO_RISK_AUDIT.md, root ROADMAP.md   │ 4 files            │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────┼────────────────────┤
  │ misc/                   │ Unreferenced 5.8 MB demo.gif                                 │ 1 file             │
  └─────────────────────────┴──────────────────────────────────────────────────────────────┴────────────────────┘

  What I kept active

  CHANGELOG.md, CLAUDE.md, README.md, SETUP.md, CODE_OF_CONDUCT.md, docs/scanner-capabilities.md, docs/security-threat-model.md,
  .claude/settings.local.json, .claude/tasks/_config.md. All actively referenced.

  Cleanup

  Removed entirely: .worktrees/ (empty), .claude/worktrees/agent-a005e29e/ (orphan subagent worktree), .superpowers/ (parent of
  brainstorm, now empty).

  Context exclusion — how it works

  - archive/ added to .gitignore (line 41). Ripgrep respects .gitignore, so the Grep tool skips it automatically.
  - Added Safety Rule #10 to CLAUDE.md: "Do not crawl archive/. For history use git log and CHANGELOG.md."
  - Explicit Read on a known archive path still works — just no accidental glob/grep discovery.
  - No .claudeignore mechanism exists in Claude Code; gitignore + CLAUDE.md rule is the canonical path.

  Archive README

  archive/README.md documents everything: directory map, keep-vs-archive rationale, impact on Agent CC's board (all 43 completed
  milestones moved — board's Completed Milestones zone will be empty until new work is added), impact on two code-comment
  references that now point at archive paths (both comments-only, not executable), and how to restore anything.

  Git status

  66 tracked-file deletions + 2 modifications (.gitignore, CLAUDE.md) staged for review. Not committed — per your rule that I
  don't commit without explicit ask. Run git status when ready; the deletions correspond exactly to what moved into archive/.

  Code references to archived paths

  Two comments now point at archive paths (non-breaking, but stale pointers):
  - server/scanner/session-tree-builder.ts:6 → docs/superpowers/specs/2026-04-12-session-hierarchy-design.md
  - client/src/components/analytics/charts/session-patterns/SessionDepthDistribution.tsx:5 →
  .claude/roadmap/charts-enrichment/charts-enrichment-task004.md

  ---
  Job 2 — Codebase Audit Report (READ-ONLY)

  Three parallel audit agents (dead code / abandoned residue / duplication+inconsistency) plus my own config/CI audit. Findings
  ordered by confidence × impact.

  Summary

  Overall health is good. 10 days of rapid rewrites left surprisingly little residue — most major transitions (pipeline removal,
  page consolidations, nerve-center topology→graph, scanner deepening, flat-to-tree migration) were executed cleanly. The biggest
  wins are formatting consolidation and a handful of unreachable pages.

  ┌──────────────────────────────┬────────────────────────┬──────────────────┐
  │           Category           │    Confirmed items     │  Effort to fix   │
  ├──────────────────────────────┼────────────────────────┼──────────────────┤
  │ Dead pages/files             │ 4                      │ ~1 hour          │
  ├──────────────────────────────┼────────────────────────┼──────────────────┤
  │ Duplicate hooks              │ 1 (3 exports)          │ 15 min           │
  ├──────────────────────────────┼────────────────────────┼──────────────────┤
  │ Duplicated formatting utils  │ ~30 call sites         │ ~1 hour          │
  ├──────────────────────────────┼────────────────────────┼──────────────────┤
  │ Error-handling inconsistency │ 13 of 25 route files   │ ~1 hour          │
  ├──────────────────────────────┼────────────────────────┼──────────────────┤
  │ Dead CI / config             │ 3 items                │ 15 min           │
  ├──────────────────────────────┼────────────────────────┼──────────────────┤
  │ Test file redundancy         │ possible, needs review │ 1-2 hours review │
  └──────────────────────────────┴────────────────────────┴──────────────────┘

  CRITICAL — verified dead code (safe to delete)

  1. client/src/pages/board.tsx (~800 lines) — unreachable
  - App.tsx line 26: const BoardPage = lazy(() => import("@/pages/board"));
  - App.tsx line 65-67: <Route path="/board"><Redirect to="/projects" /></Route> — the only /board route is a redirect, so
  BoardPage is imported but never rendered.
  - Action: Delete pages/board.tsx, remove the lazy import on App.tsx:26.

  2. client/src/pages/sessions.tsx (~400 lines) — unreachable
  - No import found anywhere in client/src.
  - App.tsx line 92-94: <Route path="/sessions"><Redirect to="/analytics?tab=sessions" /></Route>.
  - Action: Delete pages/sessions.tsx.

  3. Duplicate prompt hooks — client/src/hooks/use-sessions.ts:229-263
  - usePromptTemplates, useCreatePrompt, useDeletePrompt exist in both use-sessions.ts (lines 229, 235, 250) and use-prompts.ts
  (lines 8, 12, 42).
  - session-analytics-panel.tsx imports from use-sessions; prompts-panel.tsx imports from use-prompts.
  - use-prompts.ts is the canonical location (also has useUpdatePrompt which use-sessions lacks).
  - Action: Delete the three exports from use-sessions.ts:229-263; change session-analytics-panel.tsx import to use-prompts.

  4. client/src/components/onboarding-wizard.tsx (~1000 lines) — disabled
  - Only importer is App.tsx:15, and that import is commented out: // import { OnboardingWizard } from
  "@/components/onboarding-wizard";
  - Comment says "OnboardingWizard disabled — will be rewritten later".
  - Action: Decide — delete now (git history preserves it) or archive to archive/ with a note.

  HIGH — duplicated formatting logic (big consolidation wins)

  5. formatTokens — 16 near-identical implementations
  Every chart component, costs tab component, and a few pages have their own local formatTokens that does n >= 1M ? "…M" : n >= 1K
   ? "…K" : …. Locations include: pages/stats.tsx, pages/sessions.tsx, analytics/charts/token-economics/ModelDistribution.tsx,
  CacheEfficiencyOverTime.tsx, analytics/costs/CostsTab.tsx, SystemPromptOverhead.tsx, ModelIntelligence.tsx, TokenAnatomy.tsx,
  and 8 more.
  - Action: Export one formatTokens(n, precision?) from client/src/lib/utils.ts, replace 16 locals.

  6. formatUsd / cost formatting — 12+ near-identical implementations
  Same pattern across CostsTab.tsx, SystemPromptOverhead.tsx, ModelIntelligence.tsx, TokenAnatomy.tsx, SessionProjectValue.tsx,
  pages/sessions.tsx, pages/stats.tsx, etc. Subtle differences in decimal thresholds (2 / 3 / 4) make display inconsistent across
  pages.
  - Action: Single formatUsd in lib/utils.ts (or re-export from components/board/session-indicators.tsx which already has one).

  7. Model name normalization — 2 implementations
  - client/src/lib/utils.ts:30 — shortModel() (canonical)
  - client/src/components/board/session-indicators.tsx:58-64 — shortenModel() (near-clone)
  - Action: Delete shortenModel, import shortModel from utils.

  8. Health color mapping — 4 separate enums and palettes
  - components/health-indicator.tsx:3-8 — ok | warning | error | unknown
  - components/board/session-indicators.tsx:37-45 — good | fair | poor
  - components/analytics/sessions/SessionRow.tsx:17-24 — same as above, but re-implemented
  - components/board/project-card.tsx:28-40 — healthy | warning | critical | unknown
  - Users see different color semantics on different pages.
  - Action: Pick one enum, one helper in lib/utils.ts (maybe two helpers if project-health and session-health genuinely differ).

  HIGH — error handling inconsistency across 13 of 25 route files

  Server routes use at least three different error patterns: some return 400, others 500 for the same class of failure; some log
  with console.error, others don't; shape of the JSON response varies ({message, error} vs {message} alone). ~43
  console.error/log/warn calls across 12 of 25 route files; 13 route files have catch blocks that write error responses. Two
  route files serve non-JSON surfaces (scanner.ts and board.ts contain SSE streams via res.write) and must be partially exempted.
  - Action: Small helper handleRouteError(res, err, context) in server/lib/ consumed everywhere. Standardize on 400
  (validation) vs 500 (internal) distinction.

  MEDIUM — smaller dead code & layout issues

  9. /api/tasks orphan cache key — client/src/lib/queryClient.ts:4. Still in invalidateDataQueries but no backend endpoint matches
   (only /api/board/tasks/* exists). Harmless; delete the string.

  10. client/src/pages/prompts-panel.tsx in wrong directory — Extracted from the deleted message-history.tsx during
  messages-redesign. It's a component imported by library.tsx, not a route page. Living in pages/ violates the page/component
  convention.
  - Action: Move to client/src/components/library/prompts-panel.tsx.

  11. client/src/pages/activity.tsx and pages/prompts.tsx — redirect-only files
  - Each is 6 lines of <Redirect>. Could be inlined into App.tsx routes like the other redirects (/skills, /plugins, /mcps inline
  their redirects directly on App.tsx:71-85).
  - Low priority, but consistent with how the rest of the nav-restructure milestone shipped.

  12. use-scanner.ts:85 — stale comment
  - Comment says "Used by ScannerBrain to display last scan time…" but ScannerBrain was deleted during nerve-center-redesign.
  - Action: Update or remove the comment.

  13. server/routes/discover.ts — two helper exports only used in tests
  - buildGitHubQuery() (line 24) and ensureLibraryDir() (line 71) are exported but only referenced from
  tests/discover-routes.test.ts; buildGitHubQuery is also called locally at line 99.
  - Action: Either make them function (not export) and test through the route, or document them as internal-API-for-tests.

  MEDIUM — config / CI / scripts

  14. Two script directories — script/ vs scripts/
  - script/ (singular) holds only build.ts, referenced by package.json:27 ("build": "tsx script/build.ts").
  - scripts/ (plural) holds deploy.sh, clear-test-tasks.sh, load-test-tasks.sh.
  - The naming inconsistency is pure cruft — move script/build.ts into scripts/, update package.json.

  15. scripts/load-test-tasks.sh and clear-test-tasks.sh — reference removed pipeline
  - load-test-tasks.sh:2 comment: "Creates a dummy project with a milestone and tasks for testing the pipeline board." References
  the pipeline feature that was deleted in the pipeline-removal milestone.
  - Uses .claude/tasks/ path instead of current .claude/roadmap/.
  - Action: Delete both shell scripts.

  16. GitHub workflow residue from public-repo days
  - .github/workflows/release.yml — triggers on v* tags, but package.json has "private": true, so npm publish is impossible.
  Likely inherited from the original Claude Command Center fork. Build step still runs, but what does it do without a publish
  target?
  - .github/workflows/scorecard.yml — OSSF Scorecard is designed for public OSS repos. On a private repo, it runs but its output
  isn't externally useful.
  - Both are inherited from the upstream fork. Safe to delete.
  - ci.yml, codeql.yml, dependency-review.yml are fine.

  17. .update-prefs.json — {"enabled": false, "autoUpdate": false, "dismissedCommit": null} — empty config for an auto-updater
  that was presumably deleted from the original fork. No code references it. Safe to delete.

  LOW — test file hygiene (needs human review before acting)

  Test directory has 156 files — several show signs of iteration overlap:

  - Possible duplicate pair: tests/sessions-tab.test.ts vs tests/sessions-tabs.test.ts (singular/plural)
  - Possible duplicate pair: tests/library-tab-migration.test.ts vs tests/library-tabs-migration.test.ts (singular/plural)
  - 13 library-*.test.ts files from multiple Library iterations (library-redesign, library-polish, library-incoming,
  library-config-management) — possibly some are now redundant
  - Several analytics-*.test.ts files spanning analytics-restructure, analytics-flatten-tabs, analytics-remove-outgoing,
  analytics-overview, analytics-charts-tab, analytics-nerve-center-subtabs — some may be testing earlier iterations that have
  since been rewritten
  - phase1-fixes.test.ts — phase 1 was the very first milestone; possibly obsolete

  Action: Needs a dedicated test-audit pass — diff each pair, delete the loser, merge any stale-iteration assertions into the
  current equivalent. I didn't confirm each case because tests weren't part of job 2's scope; flagging for a future cleanup
  session.

  LOW — active CLAUDE.md could be trimmed

  Not dead, but worth noting: CLAUDE.md's Tests section (starts at line 126) is a ~3 KB paragraph that narrates the history of
  every milestone's test additions ("…wave1 (session-analytics/project-value/enricher SessionTree migration with subagent spend
  inclusion, null-tree fallback preserved…"). That historical narrative is now in the archive/ CHANGELOG + git log. The Tests
  section could be reduced to "5,682 tests across 155 files" + a one-paragraph description of what categories exist, saving ~2 KB
  from every session's loaded context.

  SILVER LININGS — audit clean on

  - Pipeline removal — zero residue in server/client/shared (only a graceful db migration at server/db.ts:181-183 that drops
  legacy pipelineConfig/pipelineRun keys — working as intended).
  - Page consolidations (Skills, Plugins, MCP, Agents, FileEditor → Library) — all redirects wired in App.tsx, no orphan page
  components.
  - Nerve center topology → EntityGraph rewrite — all topology components (ScannerBrain, CostNerves, SessionVitals, FileSensors,
  ActivityReflexes, ServiceSynapses, OrganModule) cleanly deleted; tests assert their absence.
  - Flat-to-tree migration — the three migrated consumers (session-analytics.ts, session-project-value.ts, session-enricher.ts)
  have flat fallback paths, but those are intentional graceful degradation per CLAUDE.md Rule #7, not residue.
  - ConversationNode.conversationTree — marked @deprecated in shared/session-types.ts:234, retained for test compat with zero
  production consumers. Intentional.
  - State management, styling, file naming — uniformly consistent. No churn debt there.

  Suggested refactor order

  If you want to start knocking things down, here's the highest-impact-per-hour sequence:

  1. 15 min: Delete pages/board.tsx, pages/sessions.tsx, remove orphan BoardPage lazy import, delete duplicate prompt hooks in
  use-sessions.ts, fix session-analytics-panel.tsx import. Reclaims ~1200 LOC and eliminates a class of "which file is canonical?"
   confusion.
  2. 45 min: Consolidate formatTokens and formatUsd into lib/utils.ts. Touches ~30 files but all mechanical. Eliminates the
  biggest duplication source.
  3. 30 min: Delete .github/workflows/release.yml, scorecard.yml, .update-prefs.json, scripts/{load,clear}-test-tasks.sh,
  consolidate script/ → scripts/. Small CI + config cleanup.
  4. 1 hour: Error handling helper + adoption across routes. Bigger but high-value for future debugging.
  5. 1 hour: Health color mapping consolidation, shortModel/shortenModel dedup, onboarding-wizard decision, prompts-panel.tsx
  relocation, stale comment sweep. Grab-bag polish.
  6. Later / separate session: Test file audit — the duplicates and iteration-overlap cases need more careful review than this
  pass.

  Total for items 1-5: ~3.5 hours of mostly mechanical refactoring for a meaningful codebase simplification.

  ---
  Job 1 archive is staged but uncommitted — review with git status and commit when ready. Nothing in job 2 has been touched.

✻ Brewed for 16m 52s

✻ Running scheduled task (Apr 12 10:56pm)

● Report already delivered in the previous turn — job 1 archive summary and job 2 audit findings are complete. Nothing to
  re-synthesize. Ending the loop.